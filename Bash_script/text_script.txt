#!/bin/bash
echo "Хорошо ли Вы знаете SQL? Давайте проверим"
echo "Вопрос
Что такое реляционные базы данных?
3-База данных, в которой информация хранится в виде двумерных таблиц, связанных между собой    2-Любая база данных - реляционная    1-Совокупность данных, не связанных между собой"
read a
echo "Вопрос
Как выглядит запрос, для вывода ВСЕХ значений из таблицы Orders?
3-select * from Orders   2-select ALL from Orders   1-select % from Orders"
read b
echo "Вопрос
Для чего используется LIMIT: select * from Orders limit 10?
3-необходим, чтобы показать первых 10 записей в запросе   2-необходим, чтобы показать рандомные 10 записей в запрос 3-не существует такого оператора"
read c
let scores=$a+$b+$c
#scores=$(($a + $b + $c))
if (( "$scores" > 8 )); then
        echo "Ваш результат $scores
        Умичка,все знаешь!"
elif (( "$scores" > 5 )); then
        echo "Ваш результат $scores
        Вы что-то знаете,но есть ошибки!"
else
        echo "Ваш результат $scores
        Провалено!"
fi

